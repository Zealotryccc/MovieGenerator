from docx import Document
from docx.shared import Pt, Inches, RGBColor
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.enum.style import WD_STYLE_TYPE
import os

doc = Document()

style = doc.styles['Normal']
font = style.font
font.name = 'Times New Roman'
font.size = Pt(14)

for section in doc.sections:
    section.top_margin = Inches(1)
    section.bottom_margin = Inches(1)
    section.left_margin = Inches(1)
    section.right_margin = Inches(0.6)


def h(text, level=1):
    doc.add_heading(text, level=level)
    doc.paragraphs[-1].runs[0].font.color.rgb = RGBColor(0, 0, 0)


def p(text=''):
    doc.add_paragraph(text)


def b(text):
    doc.add_paragraph(text, style='List Bullet')


# ===================== Титульный лист =====================
for _ in range(6):
    p()
h('MovieGenerator', level=2)
p('Приложение для просмотра и каталогизации фильмов')
p('Документация проекта')
for _ in range(6):
    p()
p()

# ===================== 1. ТЗ =====================
h('1. Техническое задание (ГОСТ 19.201)', 1)

h('1.1 Назначение программы', 2)
p('Программа MovieGenerator предназначена для просмотра, поиска и каталогизации фильмов. Пользователь может просматривать список фильмов, фильтровать по жанрам, искать по названию, просматривать детальную информацию о фильме, оставлять отзывы, добавлять фильмы в избранное и получать случайные рекомендации. Программа не требует регистрации — идентификация пользователя осуществляется через гостевую сессию.')

h('1.2 Требования к функционалу', 2)

h('Клиентская часть (мобильное приложение)', 3)
b('Отображение списка фильмов в виде сетки (2 колонки)')
b('Поиск фильмов по названию с debounce 350 мс')
b('Фильтрация фильмов по жанрам')
b('Детальная информация о фильме (постер, описание, актёры, режиссёр, рейтинг, длительность, возрастной рейтинг)')
b('Добавление/удаление фильмов в избранное')
b('Просмотр списка избранных фильмов')
b('Получение случайного фильма')
b('Просмотр статистики по фильмам (общая и по жанрам)')
b('Оставление отзывов с оценкой от 1 до 10')
b('Поддержка тёмной и светлой темы')

h('Серверная часть (REST API)', 3)
b('CRUD-операции для фильмов через REST API')
b('Эндпоинты для жанров, стран, актёров, тегов')
b('Поддержка пагинации и фильтрации')
b('Обработка гостевых сессий')
b('Эндпоинты: случайный фильм, популярные, статистика')

h('1.3 Требования к надёжности', 2)
b('Корректная обработка ошибок сети и сервера')
b('Понятные сообщения об ошибках для пользователя')
b('Сохранение гостевой сессии на устройстве')
b('Корректные HTTP-статусы (200, 201, 204, 400, 404, 500)')
b('Настроенные CORS-заголовки для веб-версии')

h('1.4 Требования к оборудованию', 2)

h('Серверная часть', 3)
b('ОС: Linux (Ubuntu 22.04+), Windows, macOS')
b('Python: версия 3.12+')
b('ОЗУ: от 512 МБ')
b('Дисковое пространство: от 100 МБ')

h('Клиентская часть (Android)', 3)
b('Android: версия 7.0+ (API 24+)')
b('ОЗУ: от 3 ГБ')

h('Клиентская часть (веб)', 3)
b('Любой современный браузер (Chrome, Firefox, Safari, Edge)')
b('Node.js 18+ для разработки')

h('1.5 Стадии разработки', 2)
b('Проектирование и архитектура — определение структуры БД, проектирование API, выбор стека')
b('Разработка серверной части — настройка Django, модели, ViewSetы, сериализаторы, сервисы')
b('Разработка клиентской части — настройка Expo, API-клиент, экраны, компоненты, темизация')
b('Интеграция и тестирование — интеграционное тестирование API, тестирование UI')
b('Сборка и развёртывание — EAS Build для APK, деплой Django на VPS, настройка CORS')

# ===================== 2. ПЗ =====================
h('2. Пояснительная записка (ГОСТ 19.404)', 1)

h('2.1 Введение', 2)
p('Настоящий документ содержит описание программного продукта MovieGenerator — приложения для просмотра и каталогизации фильмов. Приложение разработано как full-stack решение, включающее серверную часть на Django REST Framework и клиентскую часть на React Native (Expo).')

h('2.2 Назначение и область применения', 2)
b('Просмотр каталога фильмов с фильтрацией по жанрам и поиском')
b('Получение рекомендаций через случайный выбор фильма')
b('Ведение персонального списка избранного без регистрации')
b('Оставление отзывов и оценок к фильмам')
b('Просмотр статистики каталога (по жанрам, годам, актёрам, странам)')
p('Область применения — мобильные устройства на Android, а также веб-браузеры.')

h('2.3 Описание алгоритмов', 2)

h('Алгоритм работы с гостевой сессией', 3)
b('При первом запуске генерируется уникальный ключ гостевой сессии на основе Constants.installationId')
b('Ключ сохраняется в памяти приложения и отправляется в каждом запросе в заголовке X-Guest-Session')
b('Сервер идентифицирует пользователя по этому ключу для операций с избранным и отзывами')

h('Алгоритм поиска и фильтрации', 3)
b('Пользователь вводит поисковый запрос или выбирает жанр')
b('Клиент отправляет GET-запрос с параметрами search и genre')
b('Сервер фильтрует queryset через MovieService с использованием Q-объектов Django ORM')
b('Результат возвращается в JSON-формате')

h('Алгоритм случайного фильма', 3)
b('Клиент отправляет GET-запрос на эндпоинт movies/random/')
b('Сервер получает случайный фильм через Movie.objects.order_by("?").first()')
b('Результат возвращается через MovieDetailSerializer')

h('Алгоритм автодетекта API URL', 3)
b('Клиент проверяет наличие переменной окружения EXPO_PUBLIC_API_URL')
b('Если переменная не задана, URL определяется автоматически: для Android эмулятора — 10.0.2.2, для реального устройства — через hostUri от Metro bundler, для веба — window.location.origin')

h('2.4 Описание структуры программы', 2)

h('Серверная часть (Python/Django)', 3)
p('config/config/settings.py — настройки Django')
p('config/config/urls.py — корневые URL')
p('config/movie_generator/models.py — модели: Country, Genre, Actor, Movie, Review, Tag, UserFavorite')
p('config/movie_generator/views.py — ViewSetы REST API')
p('config/movie_generator/serializers.py — сериализаторы')
p('config/movie_generator/services.py — сервисный слой')
p('config/movie_generator/urls.py — маршруты API')
p('config/movie_generator/session_utils.py — гостевая сессия')
p('config/manage.py — утилита управления')
p('config/db.sqlite3 — база данных')

h('Клиентская часть (React Native/Expo)', 3)
p('client/src/app/ — Expo Router (файловая маршрутизация)')
p('client/src/api/ — API-клиент (config.ts, client.ts, movieApi.ts, guestSession.ts, types.ts)')
p('client/src/components/ — переиспользуемые компоненты')
p('client/src/constants/ — константы (тема, текст)')
p('client/src/hooks/ — хуки')
p('client/src/styles/ — стили')
p('client/app.json — конфигурация Expo')
p('client/eas.json — конфигурация EAS Build')
p('client/metro.config.js — конфигурация Metro с прокси API')

h('2.5 Используемые технологии', 2)

h('Серверная часть', 3)
b('Python 3.12 — язык программирования')
b('Django 6.0.5 — веб-фреймворк')
b('Django REST Framework 3.17.1 — REST API')
b('django-cors-headers — CORS-заголовки')
b('SQLite — СУБД')
b('pytest — тестирование')

h('Клиентская часть', 3)
b('TypeScript — язык программирования')
b('React Native 0.85.3 — мобильный фреймворк')
b('Expo SDK 56 — платформа разработки')
b('expo-router — файловая маршрутизация')
b('react-native-reanimated — анимации')
b('react-native-gesture-handler — жесты')
b('expo-image — оптимизированные изображения')

h('Инструменты', 3)
b('Node.js 24 — среда выполнения JavaScript')
b('EAS Build — облачная сборка APK')
b('Gradle — система сборки Android')
b('Metro bundler — JavaScript-бандлер')

h('2.6 Результаты тестирования', 2)

h('Серверная часть (pytest)', 3)
b('Реализованы тесты для моделей, API-эндпоинтов и сервисного слоя')
b('API-эндпоинты возвращают корректные HTTP-статусы')
b('Фильтрация и поиск работают согласно спецификации')

h('Клиентская часть', 3)
b('Проверена загрузка данных с сервера')
b('Проверена работа фильтрации по жанрам')
b('Проверена работа поиска по названию')
b('Проверена работа избранного и отзывов')
b('Проверена работа случайного фильма')
b('Проверена адаптация под разные размеры экрана')

h('Известные ограничения', 3)
b('Для работы веб-версии требуются CORS-заголовки на сервере')
b('Для Android эмулятора используется 10.0.2.2 для доступа к localhost')
b('При использовании EAS Build требуется интернет и аккаунт Expo')

# ===================== 3. Описание программы =====================
h('3. Описание программы (ГОСТ 19.402)', 1)

h('3.1 Наименование программы', 2)
b('Полное наименование: MovieGenerator — приложение для просмотра и каталогизации фильмов')
b('Краткое наименование: MovieGenerator')
b('Версия: 1.0.0')

h('3.2 Назначение', 2)
p('Приложение предназначено для каталогизации, поиска и просмотра фильмов. Позволяет просматривать коллекцию фильмов, фильтровать по жанрам, искать по названию, получать случайные рекомендации, добавлять фильмы в избранное и оставлять отзывы. Не требует регистрации — использует гостевую сессию.')

h('3.3 Функции', 2)
b('Просмотр списка фильмов в виде сетки 2 колонки с постерами')
b('Текстовый поиск по названию с debounce 350 мс')
b('Фильтрация по жанрам')
b('Детальная информация о фильме (постер, описание, режиссёр, актёры, длительность, возрастной рейтинг, жанры)')
b('Избранное — добавление и удаление фильмов')
b('Случайный фильм — получение рекомендации')
b('Отзывы — с оценкой от 1 до 10')
b('Статистика — общая и по жанрам, странам, годам')
b('Темизация — светлая и тёмная тема')

h('API-эндпоинты', 3)
b('GET movies/ — список фильмов с фильтрацией и поиском')
b('GET movies/:id/ — детальная информация')
b('POST movies/:id/add_review/ — добавление отзыва')
b('GET genres/ — список жанров')
b('GET actors/ — список актёров')
b('GET countries/ — список стран')
b('GET tags/ — список тегов')
b('GET movies/random/ — случайный фильм')
b('GET popular/ — популярные фильмы')
b('GET/POST favorites/ — избранное')
b('DELETE favorites/:id/ — удалить из избранного')
b('GET statistics/ — общая статистика')

h('3.4 Условия выполнения', 2)

h('Минимальные системные требования', 3)

h('Сервер', 4)
b('Python 3.12+')
b('ОС: Linux, Windows, macOS')
b('ОЗУ: от 512 МБ')

h('Клиент (Android)', 4)
b('Android 7.0+ (API 24+)')
b('ОЗУ: от 3 ГБ')
b('Expo Go или собранный APK')

h('Разработка', 4)
b('Node.js 18+')
b('Expo CLI 56+')
b('Android Studio (для сборки APK)')

h('3.5 Входные и выходные данные', 2)

h('Входные данные', 3)
b('Поисковый запрос (строка)')
b('Выбранный жанр (ID жанра)')
b('Данные отзыва (текст, оценка, автор)')
b('ID фильма для добавления в избранное')

h('Выходные данные', 3)
b('Список фильмов в JSON-формате')
b('Детальная информация о фильме')
b('Статистические данные')
b('Сообщения об ошибках')
b('Пользовательский интерфейс приложения')

# ===================== 4. Текст программы =====================
h('4. Текст программы (ГОСТ 19.401)', 1)

p('Исходный код приложения находится в следующих директориях:')
b('config/movie_generator/ — серверная часть (Python/Django)')
b('client/src/ — клиентская часть (TypeScript/React Native)')
p()
p('Основные модули серверной части:')

h('models.py', 2)
p('Определяет модели данных: Country (страна), Genre (жанр), Actor (актёр), Movie (фильм), UserFavorite (избранное), Review (отзыв), Tag (тег). Все модели наследуются от django.db.models.Model.')

h('views.py', 2)
p('Содержит ViewSetы: MovieViewSet (CRUD + random, popular, top_rated, similar, favorites, reviews), FavoriteViewSet, ReviewViewSet, StatisticsViewSet. Использует Django REST Framework.')

h('serializers.py', 2)
p('Содержит сериализаторы: MovieListSerializer, MovieDetailSerializer, MovieCreateUpdateSerializer, ReviewSerializer, UserFavoriteSerializer. Определяют формат JSON-ответов.')

h('services.py', 2)
p('Содержит сервисный слой: MovieService (фильтрация, поиск, популярные, похожие), ReviewService, FavoriteService, StatisticsService.')

p()
p('Основные модули клиентской части:')

h('src/api/client.ts', 2)
p('HTTP-клиент. Содержит функции apiRequest и unwrapList. apiRequest выполняет fetch-запросы с заголовком X-Guest-Session. unwrapList извлекает данные из ответа (поддерживает пагинацию).')

h('src/api/config.ts', 2)
p('Конфигурация API URL. Определяет адрес сервера: через переменную окружения EXPO_PUBLIC_API_URL или автоопределение по платформе.')

h('src/api/movieApi.ts', 2)
p('Методы для взаимодействия с API: getMovies, getMovie, getRandomMovie, getPopular, getGenres, getFavorites, addFavorite, addReview, getStatistics.')

h('src/app/(tabs)/index.tsx', 2)
p('Главный экран приложения. Содержит сетку фильмов (FlatList с numColumns=2), поиск с debounce, фильтр по жанрам, pull-to-refresh.')

h('src/components/movie-card.tsx', 2)
p('Компонент карточки фильма. Отображает постер, название, страну, год, жанры. Нажатие ведёт на детальный экран фильма.')

# ===================== 5. Руководство пользователя =====================
h('5. Руководство пользователя (ГОСТ 19.505)', 1)

h('5.1 Установка', 2)

h('Установка на Android через APK', 3)
b('Скачайте APK-файл')
b('Откройте файл на устройстве и подтвердите установку')
b('В настройках разрешите установку из неизвестных источников')

h('Установка на Android через Expo Go', 3)
b('Установите Expo Go из Google Play')
b('Отсканируйте QR-код из терминала Expo')

h('Запуск сервера (для разработчика)', 3)
b('Установите Python 3.12+')
b('Создайте виртуальное окружение: python -m venv .venv')
b('Установите зависимости: pip install -r req.txt')
b('Запустите: python manage.py runserver 0.0.0.0:8000')

h('5.2 Запуск', 2)
p('Для запуска мобильного приложения: установите APK на устройство и откройте его. Для запуска через Expo: выполните npx expo start в директории client и отсканируйте QR-код.')

h('5.3 Работа с программой', 2)

h('Главный экран', 3)
p('На главном экране отображается список фильмов в виде сетки. Вверху расположены строка поиска и фильтр по жанрам. Потяните список вниз для обновления.')

h('Поиск', 3)
p('Введите текст в строку поиска. Результаты обновляются автоматически через 350 мс после окончания ввода.')

h('Фильтрация по жанрам', 3)
p('Нажмите на жанр в горизонтальном списке для фильтрации. Нажмите "Все" для сброса фильтра.')

h('Детальная информация', 3)
p('Нажмите на карточку фильма для просмотра детальной информации: описание, режиссёр, актёры, рейтинг, отзывы.')

h('Избранное', 3)
p('На вкладке "Избранное" отображаются фильмы, добавленные в избранное. Нажмите на кнопку сердечка для добавления/удаления.')

h('Случайный фильм', 3)
p('На вкладке "Случайный" отображается случайный фильм из каталога. Нажмите кнопку для получения нового.')

h('Статистика', 3)
p('На вкладке "Исследовать" отображается статистика: общее количество фильмов, распределение по жанрам, актёрам, годам.')

h('5.4 Описание интерфейса', 2)
p('Интерфейс состоит из нижней панели навигации с 4 вкладками: Главная, Избранное, Случайный, Исследовать. Поддерживает тёмную и светлую тему, которая автоматически подстраивается под системные настройки.')

h('5.5 Возможные ошибки', 2)
b('"Нет связи с сервером" — проверьте, запущен ли Django сервер')
b('"Failed to fetch" — проблема с сетью или CORS')
b('Бесконечная загрузка — проверьте API URL в конфигурации')
b('JSON ожидался — сервер вернул неверный формат данных')

# ===================== 6. Руководство программиста =====================
h('6. Руководство программиста (ГОСТ 19.504)', 1)

h('6.1 Структура проекта', 2)
b('config/ — серверная часть Django')
b('config/config/ — настройки Django (settings.py, urls.py)')
b('config/movie_generator/ — приложение (models, views, serializers, services)')
b('config/db.sqlite3 — база данных')
b('config/req.txt — зависимости Python')
b('client/ — клиентская часть Expo/React Native')
b('client/src/app/ — экраны (Expo Router)')
b('client/src/api/ — API-клиент')
b('client/src/components/ — компоненты')
b('client/src/constants/ — константы и тема')
b('client/src/styles/ — стили')

h('6.2 Используемые библиотеки', 2)

h('Python', 3)
b('Django 6.0.5 — веб-фреймворк')
b('djangorestframework 3.17.1 — REST API')
b('django-cors-headers 4.9.0 — CORS')
b('pytest 9.0.3 — тестирование')
b('sqlparse 0.5.5 — форматирование SQL')

h('JavaScript/TypeScript', 3)
b('expo ~56.0.6 — платформа')
b('react-native 0.85.3 — мобильный фреймворк')
b('react-native-reanimated 4.3.1 — анимации')
b('react-native-gesture-handler ~2.31.1 — жесты')
b('expo-router ~56.2.7 — маршрутизация')
b('typescript ~6.0.3 — типизация')

h('6.3 Настройка окружения', 2)

h('Серверная часть', 3)
p('git clone <репозиторий>')
p('cd MovieGenerator')
p('python -m venv .venv')
p('.venv\\Scripts\\activate')
p('pip install -r req.txt')
p('cd config')
p('python manage.py migrate')
p('python manage.py runserver 0.0.0.0:8000')

h('Клиентская часть', 3)
p('cd client')
p('npm install')
p('npx expo start')

h('Настройка переменных окружения', 3)
p('Создайте файл client/.env:')
p('EXPO_PUBLIC_API_URL=http://<IP>:8000/movie_generator')
p('Для Android эмулятора используйте http://10.0.2.2:8000/movie_generator')

h('6.4 Сборка проекта', 2)

h('Сборка APK через EAS Build', 3)
p('eas build -p android --profile preview')

h('Сборка debug APK локально', 3)
p('cd client/android')
p('gradlew assembleDebug')

h('Сборка release APK', 3)
p('gradlew assembleRelease')

h('Запуск в эмуляторе', 3)
b('npx expo start')
b('Нажмите a для открытия в Android эмуляторе')
b('Или: npx expo run:android')

h('6.5 Модификация проекта', 2)

h('Добавление новой модели', 3)
b('Добавьте модель в config/movie_generator/models.py')
b('Выполните: python manage.py makemigrations && python manage.py migrate')
b('Добавьте ViewSet и сериализатор')
b('Зарегистрируйте маршрут в urls.py')

h('Добавление нового экрана', 3)
b('Создайте файл в client/src/app/')
b('Используйте Expo Router для маршрутизации')
b('Добавьте компоненты в client/src/components/')
b('Обновите API-клиент при необходимости')

h('Добавление фильмов в БД', 3)
b('Используйте скрипт config/seed_movies.py')
b('Или через админ-панель Django по адресу /admin/')
b('Или через API: POST /movie_generator/movies/')

# Сохранение
output_path = os.path.join(os.path.dirname(__file__), 'MovieGenerator_docs.docx')
doc.save(output_path)
print(f'Document saved: {output_path}')
